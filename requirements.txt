# requirements.txt records the full set of dependencies for development
accelerate
codetiming
datasets
dill
hydra-core
liger-kernel
numpy<3.0.0
openai
pandas>=2.3.0,<4
peft
pyarrow>=19.0.0
pybind11
pylatexenc
pre-commit
qwen-vl-utils>=0.0.14
ray[default]
tensordict>=0.8.0,<=0.10.0,!=0.9.0
torchdata
transformers
# Qwen3.5 vLLM is installed by examples/minio3/install_qwen35_official_env.sh.
# A normal resolver pass would otherwise downgrade the official transformers 5.x commit
# because vllm==0.18.0 still declares transformers<5 in its wheel metadata.
wandb
packaging>=20.0
flash-attn==2.8.3
causal-conv1d>=1.6.2.post1
flash-linear-attention>=0.5.0
uvicorn
fastapi
latex2sympy2_extended
math_verify
tensorboard
